# 규칙 + 분류기 하이브리드 카테고리 분류 — 실험 보고서

카드 상호명을 지출 카테고리로 분류할 때, **규칙(키워드) 단독**과 **규칙+분류기 하이브리드** 중
무엇이 나은지 같은 데이터에서 같은 지표로 재고, 그 결과와 근거를 정리한다.

## TL;DR

- 하이브리드(규칙 우선 → 규칙이 놓친 것만 분류기 → 저확신은 review)가 규칙 단독보다 낫다.
- 실제 카드셋(명확 케이스 60건)에서 **정확도 0.60 → 0.82**, **커버리지 63% → 100%**.
- 규칙의 높은 정밀도(0.95)는 유지하면서, 규칙이 놓친 롱테일의 **59%를 분류기가 자동으로 건짐**.
- confidence로 저확신을 review로 빼면 정밀도를 목표선에 맞출 수 있다. 현 데이터의 sweet spot은 **cutoff ≈ 0.45**(정밀도 0.96 / 자동화 77%).

> 표본이 작다(카드 고유 상호 92개, 그중 라벨이 명확한 60개). 방향은 뚜렷하지만
> 최종 수치 확정에는 라벨 상호 ~250개가 필요하다. 아래 숫자는 그 전제에서 읽어야 한다.

---

## 1. 문제

이 분류기는 카드 상호명을 32개 지출 카테고리(카페/음식점/편의점/해외SaaS/조세 등)로 나눈다.
카테고리 경계는 업종 세분화가 아니라 **세무상 다르게 처리되는지(사업관련성)** 기준이다.

기존 방식은 **키워드/정규식 규칙**이다. 고정 브랜드·PG·정부·해외 결제는 규칙이 거의 완벽하게
잡지만, **처음 보는 소상공인 상호(롱테일)** 는 규칙에 없어 미분류로 빠진다. 이 롱테일을
분류기로 메울 수 있는지가 실험의 질문이다.

## 2. 데이터

| 용도 | 데이터 | 규모 |
|---|---|---|
| 분류기 학습 | 공공데이터포털 소상공인 상가(상권)정보(전국) | 유효 277만 행 / 고유 상호 184만 |
| 평가(정답) | 실제 카드 이용내역 1년치의 고유 상호 | 92종(명확 60 / 애매 32). **개인정보 — 비공개** |

정답 라벨(gold)은 상호명 기준 초안이며 검증 대상이다. 애매한 건은 집계에서 분리했다.

## 3. 방법

- **분류기**: `HashingVectorizer(char_wb, 2~4gram)` + `SGDClassifier(log_loss)`. 상호명 dedup 후 학습.
- **라벨 체계**: 상가데이터의 상권업종 중분류(75종)로 학습 → `mapping.py`로 32-cat에 매핑.
  중분류로 학습 후 32-cat으로 접으면, 세부 클래스 간 혼동(한식↔중식 등)이 같은 카테고리로
  합쳐지며 정확도가 회복된다.
- **하이브리드 파이프라인**:
  1. 규칙 파이프라인이 커버하면 → 규칙 결과 채택(결정적).
  2. 규칙이 놓치면(uncertain) → 분류기 예측.
  3. 분류기 확률 `p ≥ cutoff` 면 자동채택, 아니면 review(사람).

## 4. 결과

### 4-1. 분류기 in-domain (상가데이터)

| 라벨 체계 | 클래스 | accuracy | macro-F1 |
|---|---:|---:|---:|
| 상권 대분류 | 10 | 0.806 | 0.804 |
| 상권 중분류 | 75 | 0.699 | 0.606 |
| 중분류 → 32-cat 매핑 | 17(도달) | 0.765 | 0.726 |

의료·미용·차량·음식점·편의점은 F1 0.8~0.96으로 잘 되고, 광고·사무용품처럼 이름에 단서가
없는 카테고리는 약하다(F1 0.45~0.50).

### 4-2. 실제 카드셋 3-way 비교 (명확 60건)

| 전략 | coverage | 정밀도 | accuracy |
|---|---:|---:|---:|
| 규칙-only | 0.63 | **0.95** | 0.60 |
| 분류기-only | 1.00 | 0.65 | 0.65 |
| **하이브리드** | 1.00 | 0.82 | **0.82** |

분류기 순수 기여: 규칙이 놓친 22건 중 **59%**를 분류기가 맞힘.

(전체 92건 기준으로도 하이브리드 0.66 > 분류기 0.55 > 규칙 0.43으로 순서 동일. 애매 케이스의
gold 노이즈 때문에 절대값만 낮아진다.)

### 4-3. confidence cutoff 탐색 (명확 60건)

| 목표 정밀도 | 최소 cutoff | 자동화율 | 실제 정밀도 |
|---:|---:|---:|---:|
| ≥ 0.90 | 0.23 | 87% | 0.90 |
| ≥ 0.95 | **0.45** | 77% | 0.96 |
| ≥ 0.99 | 달성 불가 | — | — |

cutoff ≈ 0.45가 knee다. 여기서 규칙이 놓친 것 중 자동채택분이 100% 정확해지고, 전체 정밀도
0.96 / 자동화 77%. 이보다 올리면 정밀도는 그대로인데 review만 늘고, 내리면 정밀도가 깎인다.

## 5. 왜 하이브리드가 더 나은가

1. **규칙의 강점(정밀도)을 그대로 둔다.** 규칙이 커버한 건 결정적·고정밀(0.95)이라 확률모델이
   덮어쓰지 않는다. 감사 가능성과 재현성이 유지된다.
2. **규칙의 약점(롱테일 미분류)을 분류기가 메운다.** 규칙이 놓친 것의 절반 이상을 자동 분류해
   커버리지를 63% → 100%로 올린다.
3. **저확신은 걸러 review로 보낸다.** 분류기가 자신 없는 예측(주로 온라인·해외처럼 학습에 없던
   분포)은 확률이 낮게 나오므로, cutoff로 잘라 정밀도 목표를 지킬 수 있다.
4. **분류기가 못 하는 영역은 규칙이 맡는다.** 오프라인 프랜차이즈(편의점·카페·패스트푸드)는
   분류기가 고확신으로 정확하지만, 해외SaaS·구독·조세·PG처럼 상가데이터에 없는 카테고리는
   구조적으로 분류기 대상이 아니다. 이건 규칙/키워드가 담당한다.

즉 하이브리드는 두 방식의 강점을 각자 맡기고 약점을 서로 메우는 구조라, 규칙 단독보다
커버리지와 정확도가 모두 높다.

## 6. 분류기의 도달 범위

분류기(상가데이터 학습)가 낼 수 있는 카테고리는 **32개 중 17개**뿐이다.

```
도달 가능 (17): 카페 음식점 편의점 차량 미용 의료 교육 광고 전자기기
               생활용품 여가 여비교통 임차료 전문가수수료 수리비 사무용품 기타
도달 불가 (15): 온라인쇼핑 음식배달 해외SaaS 국내SW 통신 수도광열 도서
               금융 지자체_과태료 경찰청_범칙금 조세 PG_미상 게임 구독서비스 보험
```

도달 불가 15개는 데이터 부족이 아니라 성격이 달라서다. 고정 브랜드(통신·금융·구독 등)는
사전 한 줄이면 되고, 거래 성격 라벨(조세·과태료·PG_미상·해외SaaS)은 "상호명 → 업종" 문제가
아니라 규칙의 영역이다.

## 7. 구조적 상한과 한계

- **전체 정밀도의 천장은 규칙이 정한다.** 규칙은 항상 자동채택되므로, 규칙 자체 정밀도가
  파이프라인 상한이다. 정밀도 ≥0.99를 원하면 분류기 cutoff가 아니라 규칙의 오분류를 손봐야 한다.
- **표본이 작다.** 카드 고유 상호 92개(명확 60), 각 cutoff 구간은 한 자릿수 샘플이다.
  cutoff 0.45는 방향일 뿐 정확한 최적값이 아니다.
- **gold는 초안 라벨**이라 검증 시 수치가 바뀔 수 있다.
- **도메인 시프트**: 학습은 깨끗한 등록 상호명, 평가는 지점명·카드사 태그가 붙은 카드 문자열이다.
  오프라인 프랜차이즈는 잘 전이되지만 온라인·해외는 그렇지 않다.

## 8. 권장 파이프라인과 다음 단계

```
거래 → 정규화 → merchant 캐시(DB) 조회
     → 규칙(고정브랜드·PG·정부·해외 키워드)
     → 분류기(오프라인 롱테일) + confidence
     → p<cutoff(≈0.45) 또는 미해소 → review
     → 결과를 캐시에 write-back(source, confidence) = 학습 루프
```

- verdict(경비 가능/불가)와 계정과목은 이 분류기가 아니라 룰카드 엔진이 판정한다.
  분류기는 category(입력 특성)만 정한다.
- **다음 단계**: 실제 카드 상호를 ~250개까지 라벨링해 3-way 비교와 cutoff를 재측정하면
  결정이 통계적으로 확정된다.

## 재현

- `eval/evaluate.py` — 예측 CSV로 3-way 지표 산출.
- `threshold` 탐색은 `eval/` 스크립트로 cutoff 스윕.
- 분류기 학습/저장은 `train.py`, 예측은 `predict.py`(README 참고).
- 실카드 평가셋은 개인정보라 저장소에 포함하지 않는다. 수치는 집계값만 기재했다.
